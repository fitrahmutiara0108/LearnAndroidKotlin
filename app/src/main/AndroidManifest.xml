<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_livinmandiri"
        android:label="MandiriApps"
        android:roundIcon="@drawable/ic_livinmandiri"
        android:supportsRtl="true"
        android:theme="@style/Theme.MandiriApps"
        tools:targetApi="31">

        <activity
            android:name=".presentation.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".presentation.home.HomeActivity"/>
        <activity android:name=".presentation.ProfileActivity"/>
        <activity android:name=".presentation.RegisterActivity"/>
        <activity android:name=".presentation.HomeMainActivity"/>
        <activity android:name=".presentation.home.HomeFragment"/>
        <activity android:name=".presentation.message.MessageFragment"/>
        <activity android:name=".presentation.ProfileFragment"/>
        <activity android:name=".presentation.PromoFragment"/>
        <activity android:name=".presentation.SettingFragment"/>
    </application>

</manifest>